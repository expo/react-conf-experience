/**
 * @providesModule ReactConfApp
 */

import React, {
  AppRegistry,
  View,
} from 'react-native';

class App extends React.Component {
  render() {
    return (
      <View />
    );
  }
}

AppRegistry.registerComponent('main', () => App);
